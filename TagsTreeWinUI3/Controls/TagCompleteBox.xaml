<UserControl
    x:Class="TagsTreeWinUI3.Controls.TagCompleteBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TagsTreeWinUI3.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:TagsTreeWinUI3.ViewModels"
    mc:Ignorable="d">
    <AutoSuggestBox
        VerticalAlignment="Center"
        LostFocus="PathComplement"
        PlaceholderText="标签路径（不包含/:*?&quot;&lt;&gt;|,和空白字符且不为空）"
        SuggestionChosen="SuggestionChosen"
        Text="{x:Bind Path, Mode=TwoWay}"
        TextChanged="PathChanged"
        UpdateTextOnSelect="False">
        <AutoSuggestBox.ItemTemplate>
            <DataTemplate x:DataType="viewModels:TagViewModel">
                <TextBlock Text="{x:Bind FullName}" />
            </DataTemplate>
        </AutoSuggestBox.ItemTemplate>
    </AutoSuggestBox>
</UserControl>
