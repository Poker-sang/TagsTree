<Page
    x:Class="TagsTreeWinUI3.Views.FileImporterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TagsTreeWinUI3"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI.UI"
    xmlns:uiControls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:viewModels="using:TagsTreeWinUI3.ViewModels"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid x:Name="FileImporterGrid">
        <uiControls:DockPanel>
            <CommandBar uiControls:DockPanel.Dock="Top">
                <AppBarButton Icon="{ui:FontIcon Glyph=&#xE7C3;}" Label="选择">
                    <AppBarButton.Flyout>
                        <MenuFlyout Placement="BottomEdgeAlignedLeft">
                            <MenuFlyoutItem
                                Command="{x:Bind _vm.Import}"
                                CommandParameter="Select_Files"
                                Icon="{ui:FontIcon Glyph=&#xE7C3;}"
                                Text="文件" />
                            <MenuFlyoutItem
                                Command="{x:Bind _vm.Import}"
                                CommandParameter="Select_Folders"
                                Icon="{ui:FontIcon Glyph=&#xF12B;}"
                                Text="文件夹" />
                        </MenuFlyout>
                    </AppBarButton.Flyout>
                </AppBarButton>
                <AppBarButton Icon="{ui:FontIcon Glyph=&#xE8F4;}" Label="目录">
                    <AppBarButton.Flyout>
                        <MenuFlyout Placement="BottomEdgeAlignedLeft">
                            <MenuFlyoutItem
                                Command="{x:Bind _vm.Import}"
                                CommandParameter="Path_Files"
                                Icon="{ui:FontIcon Glyph=&#xE7C3;}"
                                Text="文件" />
                            <MenuFlyoutItem
                                Command="{x:Bind _vm.Import}"
                                CommandParameter="Path_Folders"
                                Icon="{ui:FontIcon Glyph=&#xF12B;}"
                                Text="文件夹" />
                            <MenuFlyoutItem
                                Command="{x:Bind _vm.Import}"
                                CommandParameter="Path_Both"
                                Icon="{ui:FontIcon Glyph=&#xE71D;}"
                                Text="全部" />
                        </MenuFlyout>
                    </AppBarButton.Flyout>
                </AppBarButton>
                <AppBarButton
                    Command="{x:Bind _vm.Import}"
                    CommandParameter="All"
                    Label="全部">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE82E;" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarSeparator />
                <AppBarButton
                    Command="{x:Bind _vm.DeleteBClick}"
                    Icon="Delete"
                    Label="清空" />
                <AppBarButton
                    Command="{x:Bind _vm.SaveBClick}"
                    CommandParameter="{Binding ElementName=FileImporterGrid}"
                    Icon="Save"
                    Label="保存" />
            </CommandBar>

            <uiControls:DataGrid
                x:Name="DataGrid"
                AutoGenerateColumns="False"
                CanUserReorderColumns="False"
                ItemsSource="{x:Bind _vm.FileViewModels}">
                <uiControls:DataGrid.Columns>
                    <uiControls:DataGridTemplateColumn
                        Width="45"
                        Header="{ui:FontIcon Glyph=&#xF12B;}"
                        IsReadOnly="True">
                        <uiControls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="viewModels:FileViewModel">
                                <Image
                                    Width="32"
                                    Height="32"
                                    Source="{x:Bind Icon}" />
                            </DataTemplate>
                        </uiControls:DataGridTemplateColumn.CellTemplate>
                    </uiControls:DataGridTemplateColumn>
                    <uiControls:DataGridTemplateColumn
                        Width="2*"
                        Header="名称"
                        IsReadOnly="True">
                        <uiControls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="viewModels:FileViewModel">
                                <TextBlock Text="{x:Bind Name}" />
                            </DataTemplate>
                        </uiControls:DataGridTemplateColumn.CellTemplate>
                    </uiControls:DataGridTemplateColumn>
                    <uiControls:DataGridTemplateColumn
                        Width="*"
                        Header="路径"
                        IsReadOnly="True">
                        <uiControls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="viewModels:FileViewModel">
                                <TextBlock Text="{x:Bind PartialPath}" />
                            </DataTemplate>
                        </uiControls:DataGridTemplateColumn.CellTemplate>
                    </uiControls:DataGridTemplateColumn>
                </uiControls:DataGrid.Columns>
            </uiControls:DataGrid>
        </uiControls:DockPanel>
    </Grid>
</Page>
